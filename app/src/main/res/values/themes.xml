<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme — Material 3, follows system dark mode.
         DynamicColors.applyToActivityIfAvailable() overlays Material You
         palettes at runtime on Android 12+. -->
    <style name="Theme.AuraOrbit" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Settings theme: Material 3 with a MaterialToolbar (NoActionBar) for
         proper edge-to-edge inset handling on Android 15+. The MaterialToolbar
         is declared in activity_settings.xml and registered via setSupportActionBar. -->
    <style name="Theme.AuraOrbit.Settings" parent="Theme.AuraOrbit">
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <!-- Sphere Mode theme: NoActionBar, fully transparent/black window background
         so the libGDX GL surface fills the entire screen without a white flash.
         Edge-to-edge is configured in SphereModeActivity via WindowCompat. -->
    <style name="Theme.AuraOrbit.SphereMode" parent="Theme.AuraOrbit">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    <style name="Theme.AuraOrbit.BlurPreviewDialog" parent="Theme.Material3.DayNight.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
    
    <style name="Theme.AuraOrbit.TransparentFullscreen" parent="Theme.AuraOrbit">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
